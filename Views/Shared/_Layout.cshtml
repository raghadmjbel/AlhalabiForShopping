<!DOCTYPE html>
<html lang="ar">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>مول الحلبي للتسوق</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root { --gold: #e0b354; }
        body { background-color: #0b0b0b; color: white; font-family: 'Cairo', sans-serif; }
        .text-gold { color: var(--gold) !important; }
        .btn-gold { background-color: var(--gold); color: black; font-weight: bold; border: none; }
        .btn-gold:hover { background-color: #c99a3c; color: white; }
        .border-gold { border: 1px solid var(--gold) !important; }
        .navbar { border-bottom: 2px solid var(--gold); background: black !important; }
    </style>
</head>
<body dir="rtl">
    <nav class="navbar navbar-expand-lg navbar-dark mb-4">
        <div class="container">
            <a class="navbar-brand text-gold fw-bold fs-3" href="/">مول الحلبي</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link text-white mx-2" href="/Product/Index"><i class="fas fa-store"></i> المتجر</a>
                <a class="nav-link text-white mx-2" href="/Cart/Index"><i class="fas fa-shopping-cart"></i> السلة</a>
                <a class="nav-link text-gold fw-bold mx-2" href="/Admin/Dashboard"><i class="fas fa-user-shield"></i> لوحة الإدارة</a>
            </div>
        </div>
    </nav>
    <div class="container">
        @RenderBody()
    </div>
</body>
</html>